{
  "name": "tradeline",
  "private": true,
  "version": "0.0.0",
  "description": "Tradeline — institutional intelligence for licensed debt buyers",
  "workspaces": [
    "apps/*",
    "mcp-servers/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
